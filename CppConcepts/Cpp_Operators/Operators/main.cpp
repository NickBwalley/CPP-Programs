#include <iostream>

using namespace std;

int main()
{
    cout << "Hello world!" << endl;
    return 0;
}
/*OPERATORS
1. Arithmetic Operators -> (+, -, *, /, %, ++, -- )
2. Assignment Operators -> (=, +=, -=, *=, /=, %= )
3. Relational Operators -> (==, !=, >, >=, <, <= )
4. Logical Operators -> (&&, ||) &&-> return true if all are true, ||-> return true if one condition is fulfilled
5. Bitwise Operators ( can be used to perform operations on individual bits, used alongside char and int datatypes)
    & -> Binary AND
    | -> Binary OR
    ^ -> Binary XOR
    ~ -> Binary Ones complement
    <<  Binary Shift Left
    >> Binary Shift Right

*/
