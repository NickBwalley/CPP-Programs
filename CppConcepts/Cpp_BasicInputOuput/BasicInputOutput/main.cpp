#include <iostream>
// Note that if we don't include the "using namespace std" then we have to use the std::cout
// Note even endl must be declared as "std::endl;"
int main()
{
    std::cout << "Hello world!" << std::endl;
    return 0;
}
